strict digraph  {
documents;
comments;
schools;
curriculum;
"student-associations";
payments;
ticketing;
links;
users;
groups;
events;
reactions;
notifications;
posts;
gitlab;
documents -> comments;
comments -> notifications;
schools -> curriculum;
"student-associations" -> payments;
ticketing -> links;
ticketing -> users;
ticketing -> payments;
users -> notifications;
users -> links;
groups -> links;
groups -> users;
events -> reactions;
events -> notifications;
events -> links;
events -> users;
events -> payments;
events -> ticketing;
posts -> notifications;
posts -> links;
gitlab -> users;
}
