query PageSettingsTheme @loading {
  themes(all: true) @list(name: "List_SettingsThemes") {
    localID
    ...ThemePreviewCard
  }

  me {
    canCreateThemesOn {
      ...PickGroup @mask_disable
    }
  }
}
