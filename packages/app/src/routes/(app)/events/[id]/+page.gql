query PageEventDetail($id: LocalID!) {
  event(id: $id) @loading {
    canScanBookings
    canEdit
    highlightedBooking: myBookings(count: 1) {
      ...CardBooking
    }
    myBookings {
      id
    }
    organizer {
      name
      uid
      ...AvatarGroup
    }
    coOrganizers {
      name
      uid
      ...AvatarGroup
    }
    title
    ...TextEventDates
    location
    descriptionHtml
    tickets {
      ...CardTicket
      ...CardTicketPlaces
    }
    ...ButtonLike
    ...ButtonShare
    links {
      ...PillLink
    }
  }
}
