query PageHomeFeed {
  homepage(first: 5) @paginate {
    pageInfo {
      hasNextPage
    }
    edges {
      node {
        id
        uid
        group {
          uid
        }
        ...CardArticle
      }
    }
  }
}
