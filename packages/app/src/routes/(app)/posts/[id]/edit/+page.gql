query PagePostEdit($id: LocalID!) {
  article(id: $id) {
    id
    slug
    group {
      uid
    }
    ...FormPicture
    ...FormArticle
  }

  me: assertMe {
    canCreatePostsOn {
      ...FormArticleGroups @mask_disable
    }
  }
}
